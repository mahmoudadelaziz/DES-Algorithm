#include <stdio.h>

/*
    Program Layout:
    > Partition of the input block into two equal halves
    > DES function of the right half and the subkey
    > XOR of the left half with the output of the function

*/

// 1. Partition the i/p plaintext into two 32-bit halves.

int main()
{

    return 0;
}
